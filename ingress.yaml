apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-campus-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: ai-campus-service
            port:
              number: 80
